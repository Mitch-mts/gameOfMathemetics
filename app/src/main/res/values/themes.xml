<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GameOfMathemetics" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/blue</item>
    </style>

    <style name="Base.Theme.Addition" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/blue_variant</item>
    </style>

    <style name="Theme.Result" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/yellow</item>
    </style>

    <style name="Theme.GameOfMathemetics" parent="Base.Theme.GameOfMathemetics" />
</resources>